import Foundation

enum Constants {
    enum Collections {
        static let users = "users"
        static let guardians = "guardians"
        static let unlockRequests = "unlockRequests"
        static let unlockSessions = "unlockSessions"
        static let restrictions = "restrictions"
        static let challenges = "challenges"
        static let groups = "groups"
    }

    enum Durations {
        static let unlockOptions = [5, 15, 30, 60]
    }
}
